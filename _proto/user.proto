syntax = "proto3";

package user;

service UsersService {
    rpc Find (User) returns (User);
}

message UserById {
    int32 id = 1;
}

message User {
    int32 id = 1;
    string name = 2;
    string email = 3;
    string password = 4;
    int32 age = 5;
}
