syntax = "proto3";

package user;


service UsersService {
  rpc FindByEmail (PayloadFindByEmail) returns (SignupInput);
  rpc Signup (SignupInput) returns (SignupInput);
}

message SignupInput {
  string email = 1;
  string password = 2;
}

message PayloadFindByEmail {
  string email = 1;
}
