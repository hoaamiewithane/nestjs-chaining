type Query {
    users: [User]
    user(id: ID!): User
}

type Mutation {
    signup(data: SignupUserInput):SignUpResponse
}


type SignUpResponse {
    accessToken: String
    refreshToken:String
}

type User {
    email: String
}

input SignupUserInput {
    email: String!
    password: String!
}
